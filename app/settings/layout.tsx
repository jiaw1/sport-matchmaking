// FILE: layout.tsx
import { ReactNode } from "react";

export const metadata = {
  title: "Updates | Sport Matchmaking",
  description: "Sport Matchmaking Settings page",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}